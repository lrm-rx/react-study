import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './01_组件定义/02_render返回值';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <App />
);
